CORE_IMAGE_BASE_INSTALL:append = " \
    ${VIRTUAL-RUNTIME_container_engine} \
    docker-compose \
    docker-compose-up \
    docker-integrity-checker \
    docker-watchdog \
    docker-auto-prune \
"
